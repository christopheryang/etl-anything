# URL of the FastAPI backend (started via `uvicorn main:app --port 8000` in backend/).
FASTAPI_URL=http://localhost:8000

# Path to the backend's uploads directory. Must match the backend's UPLOADS_DIR.
# Default assumes the standard repo layout (frontend/ and backend/ as siblings).
UPLOADS_DIR=../backend/uploads
